import { MoneyInputDirective } from './money-input.directive';

describe('MoneyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MoneyInputDirective();
    expect(directive).toBeTruthy();
  });
});
